あなたはWindows向けの業務用在庫管理アプリを開発するエンジニアです。
以下の仕様に基づいて、VSCodeで開発できるシンプルな在庫管理アプリを作成してください。

# 目的

カードショップで扱うカード在庫を管理するためのアプリを作成したいです。

主な目的は以下です。

- 現在どの商品在庫があるか把握したい
- 最初に値段を付けてから時間が経った商品を確認し、価格を見直したい
- 売れた商品を在庫一覧から除外・管理したい
- 在庫データを編集し、業務データとして活用したい
- 買取ツールなどからCSVでデータをインポートしたい
- 編集後のデータをCSVでエクスポートし、棚卸や確認用データとして使いたい
- 絞り込みやソートで検索しやすくしたい
- 外部サイトで価格確認しやすいよう、商品ごとに検索リンクを表示したい

# 開発方針

まずは複雑な機能を避け、シンプルで安定して使える初期版を作成してください。

実装は一度に全機能を作らず、Phaseを区切って段階的に進めてください。
各Phaseの完了時点で、動作確認しやすい状態にしてください。

## 実装Phase

### Phase 1：基本在庫管理

最初に作る最小構成です。

- SQLiteによる在庫データ保存
- SQLiteのテーブル作成処理
- 在庫一覧画面
- 商品追加機能
- クイック編集機能（通常編集）
- 詳細編集機能
- ステータス管理
- 物理削除しない削除済み運用
- 通常CSVのインポート
- CSVエクスポート
- サンプルCSV
- README
- 起動方法

Phase 1では、まずローカルPC上で安全に在庫を登録・編集・保存・CSV入出力できることを優先してください。

### Phase 2：検索・作業効率改善

Phase 1が安定して動作してから追加してください。

- 絞り込み機能
- ソート機能
- 価格見直し用の絞り込み
- 一括操作機能
- 最終確認日のカレンダー日付選択
- 複製機能
- 一覧表示列の選択
- 価格検索リンク機能
- 価格検索リンク設定

### Phase 3：外部連携・拡張準備

Phase 2までが安定してから追加してください。

- Manabox CSVインポート
- CSVインポート形式の自動判別
- 表示設定画面
- 将来的なタグ候補挿入UIを追加しやすい構成
- 将来的な買取見積書機能を追加しやすい構成
- 将来的に.exe化する場合の手順メモ

初回の提案では、上記Phase分割を前提に、どのPhaseでどの機能を作るかを明示してください。
実装に進む場合も、まずPhase 1から着手してください。

初期版では以下を重視します。

- 在庫データを安全に保存・編集できること
- CSVインポート/エクスポートができること
- 一覧表示、絞り込み、ソートができること
- 価格確認用リンクを開けること
- 誤編集・誤削除を防ぐため、保存前にキャンセルできること
- データ破損しにくい構成にすること

# 技術要件

- 開発環境：VSCode
- 実行環境：Windows 11
- データ保存：SQLite
- CSV：インポート/エクスポート用
- 通常の在庫データはSQLiteで管理する
- 可能であれば、将来的にPyInstallerで.exe化しやすい構成にする

使用する技術は、実装しやすく保守しやすいものを選んでください。
Pythonで作る場合は、SQLite、CSV処理、GUIまたはローカルWeb UIを使ってください。

# アプリの基本構成

以下の画面を作成してください。
ただし、以下は全Phase完了時点の最終的な画面構成です。
Phase 1では、Phase 1に必要な画面・機能を優先し、Phase 2以降の機能は後から追加できる構成にしてください。

1. 在庫一覧画面
2. 商品追加画面
3. 商品編集機能
4. 絞り込み・ソート機能
5. 一括操作機能
6. CSVインポート/エクスポート画面
7. 設定画面

# データ保存方式

通常のデータ保存にはSQLiteを使用してください。

CSVは以下の用途に限定します。

- 外部データのインポート
- 棚卸や確認用のエクスポート
- 他ツールとの連携

CSVを直接メインデータとして扱うのではなく、アプリ内部の通常データはSQLiteに保存してください。

## データ保護

データ破損を防ぐため、以下を実装方針に含めてください。

- CSVインポート前にSQLite DBのバックアップを作成する
- 大量インポートはトランザクション内で行い、エラー時はロールバックする
- DBスキーマにはバージョン番号を持たせ、将来のマイグレーションに備える
- 書き込み処理では、保存前にバリデーションを行う
- アプリ側で想定外のエラーが発生した場合は、データを中途半端に更新しない

# 在庫データ項目

在庫データには以下の項目を持たせてください。

## 必須項目

- 在庫ID
- ジャンル
- カード名
- 枚数
- 販売価格
- 在庫ステータス
- 登録日
- 最終確認日
- 更新日

## 任意項目

- レア
- セット
- 言語
- コレクター番号
- 補足
- カード状態
- 買取価格
- メモ

# 各項目の説明

## 在庫ID

- 各在庫行を一意に識別するID
- アプリ側で自動採番する
- 例：INV-000001
- CSVインポート時、在庫IDが空欄の場合は自動採番する
- 初期版では、CSVインポート時に既存の在庫IDがあっても上書きせず、新規追加扱いにする
- 複製機能で作成した在庫行にも、元データとは異なる新しい在庫IDを自動採番する

## ジャンル

カードゲームの種類を入れる項目です。

例：

- MTG
- ポケカ
- 遊戯王
- デュエマ
- ワンピースカード
- その他

## カード名

カード名を保存します。

## レア

カードのレアリティを保存します。

例：

- C
- U
- R
- MR
- SAR
- SEC

ジャンルによって表記が異なるため、初期版では自由入力で構いません。

## セット

収録セット名またはセットコードを保存します。

例：

- 4EDBB
- M4
- DM24
- その他

## 言語

カードの言語を保存します。

例：

- jp
- en

## コレクター番号

カード番号を保存します。

例：

- 055/071
- 116/083
- 123

## 補足

カードの追加情報を保存します。

補足欄は自由入力のまま維持してください。

ただし、検索や分類に使いやすい属性は `#タグ` 形式で入力することを推奨します。

例：

- `#Foil`
- `#拡張アート`
- `#プロモ`
- `#パラレル`
- `#テキストレス`
- `#サイン入り`

タグは補足欄内の通常文字列として保存し、初期版では専用のタグテーブルは作らなくて構いません。

適切なPhaseで、補足欄に入力されたスペース区切りの語句を自動でタグ化する機能を実装してください。
この機能では、補足欄の内容を保存する前、または入力確定時に、スペース区切りの各語句の先頭へ `#` を自動付与してください。
すでに `#` が付いている語句には、`#` を重複付与しないでください。

例：

- `Foil プロモ 拡張アート` → `#Foil #プロモ #拡張アート`
- `#プロモ Foil` → `#プロモ #Foil`

この自動タグ化は、ユーザーが毎回 `#` を手入力しなくても補足欄をタグとして使いやすくするための入力補助です。
ただし、Phase 1では必須実装ではなく、検索・作業効率改善を扱うPhase 2以降など、適切なタイミングで実装してください。

将来的に、商品追加、クイック編集、詳細編集の補足入力欄でタグ一覧から項目を選択し、選択したタグを補足欄へ簡単に挿入できる機能を追加しやすい構成にしてください。
スペース区切りの自動タグ化に加えて、将来的にはタグ候補から選ぶ方式も実装候補としてください。

タグ候補の例：

- `#Foil`
- `#拡張アート`
- `#プロモ`
- `#パラレル`
- `#テキストレス`
- `#サイン入り`
- `#通常版`

## カード状態

カードの状態を保存します。

例：

- A
- A-
- B
- C
- D
- SP
- MP
- HP
- Poor

初期版では自由入力で構いません。

## 枚数

同一条件の在庫枚数を保存します。

同一カードでも、以下が異なる場合は別在庫行として扱ってください。

- 言語
- カード状態
- 補足
- 販売価格
- 買取価格

完全に同一条件の場合のみ、枚数を合算できる仕様にしてください。

## 買取価格

買取時の価格を保存します。

## 販売価格

店頭販売価格を保存します。

## 在庫ステータス

在庫の状態を管理します。

初期値は「販売中」とします。

ステータスは以下を用意してください。

- 販売中
- 準備中
- 売却済み
- 取置中
- 販売停止
- 削除済み

将来的な買取見積書機能では、見積対象の商品を区別するために「査定中」などのステータス追加を検討してください。ただし、ステータス追加時は、CSV互換性や既定表示への影響を確認してから追加してください。

通常の削除操作ではデータを完全削除せず、在庫ステータスを「削除済み」に変更してください。

## 登録日

在庫を登録した日付です。

形式は以下に統一してください。

YYYY-MM-DD

## 最終確認日

価格を最後に確認した日付です。

価格見直しの判定には、この「最終確認日」を使用してください。

Phase 2以降で、最終確認日を手入力だけでなくカレンダーから日付選択できるようにしてください。

カレンダー日付選択は、少なくともクイック編集、詳細編集、最終確認日の手動更新操作で利用できるようにしてください。

形式は以下に統一してください。

YYYY-MM-DD

## 更新日

データを最後に編集した日付です。

ユーザーが手入力する項目ではなく、データを保存・更新したタイミングでアプリが自動更新してください。

形式は以下に統一してください。

YYYY-MM-DD

# 日付の扱い

日付形式は原則として以下に統一してください。

YYYY-MM-DD

CSVインポート時に以下のような表記があった場合は、可能であれば変換してください。

例：

- 3月1日
- 03/01
- 2026/03/01

変換できる場合は、YYYY-MM-DD に変換してください。

年が省略されている場合は、取り込み時点の年を補ってください。

変換できない場合は、インポート前のエラー一覧に表示してください。

# 在庫一覧画面

在庫データを一覧表示してください。

初期表示する列は以下にしてください。

- ジャンル
- カード名
- 補足
- 枚数
- 販売価格
- 最終確認日
- 在庫ステータス
- 価格検索リンク

カード名の下には、サブ情報としてセット、レア、カード状態、言語、コレクター番号を表示してください。

一覧画面では、表示する列をユーザーが選択できるようにしてください。

表示件数が多い場合は、50件ごとにページ分けしてください。

初期表示では、在庫ステータスが「販売中」の商品を表示してください。
「売却済み」「削除済み」なども、絞り込みで表示できるようにしてください。

# 商品追加機能

商品追加画面を作成してください。

入力項目は、在庫データ項目と同じです。

ただし、以下は自動設定してください。

- 在庫ID
- 登録日
- 更新日

最終確認日は、未入力の場合は登録日と同じ日付を入れてください。

在庫ステータスは、未入力の場合は「販売中」にしてください。

商品追加時、必須項目が不足している場合は保存せず、エラー表示してください。

商品追加画面にも、入力中の商品情報を使って外部サイトで価格確認できるリンクを表示してください。

商品追加画面の価格確認リンクは、在庫一覧の価格検索リンク設定と同じURLテンプレートを使ってください。まだ保存されていない入力内容から一時的に検索URLを作成し、保存前でも相場確認できるようにしてください。

# 編集機能

在庫一覧上で直接編集できるクイック編集方式にしてください。

クイック編集は、日常業務で変更頻度が高い項目だけを一覧行の中で素早く編集する機能です。
詳細な商品情報を変更する場合は、詳細編集を使用してください。

各行の編集ボタンを押すと、その行だけがクイック編集状態になり、一覧行の表示が入力欄に切り替わるようにしてください。
通常編集用の別ダイアログは開かず、一覧上で編集を完結してください。

クイック編集項目は以下です。

- 補足
- 枚数
- 販売価格
- 最終確認日
- 在庫ステータス

ジャンル、カード名、レア、セット、言語、コレクター番号、カード状態、買取価格、メモはクイック編集には含めず、詳細編集から編集できるようにしてください。

編集時は、現在の値を入力欄に表示してください。

保存ボタンを押すと、入力欄の内容で上書きしてください。
任意項目を空欄にした場合は、空欄として保存してください。
必須項目、数値項目、日付項目は空欄保存不可とし、保存前にエラー表示してください。

販売価格の入力欄には、1円単位で上下するスピンボタンを表示しないでください。

クイック編集、詳細編集、商品追加のいずれでも、価格はキーボードで直接入力する方式にしてください。

クイック編集状態の行には、保存ボタンとキャンセルボタンを表示してください。
保存ボタンを押すと、入力内容を保存して通常表示に戻してください。
キャンセルボタンを押すと、編集内容を破棄して元の表示に戻してください。
クイック編集の入力欄にフォーカスがある状態でEnterキーを押すと、保存ボタンと同じく入力内容を保存してください。
クイック編集の入力欄にフォーカスがある状態でEscキーを押すと、キャンセルボタンと同じく編集内容を破棄してください。

未保存のクイック編集がある状態で、別の行の編集ボタン、詳細編集ボタン、複製ボタン、ページ移動、表示更新、絞り込み、CSV操作など、編集内容が失われる可能性がある操作を行う場合は、確認ダイアログを表示してください。

確認メッセージ例：

「未保存の編集内容があります。破棄して別の商品を編集しますか？」

ユーザーがキャンセルした場合は、現在の編集内容を保持し、別の商品への編集切り替えは行わないでください。

ユーザーが承認した場合は、現在の編集内容を破棄して、選択した操作に切り替えてください。

誤入力や誤削除を防ぐため、保存ボタンとキャンセルボタンを必ず用意してください。

更新日は、保存時に自動で当日の日付へ更新してください。

在庫ID、登録日、更新日はクイック編集には表示せず、編集対象にも含めないでください。

最終確認日は編集可能にしてください。

## 詳細編集機能

在庫一覧の各行に、クイック編集用の「編集」ボタンとは別に「詳細編集」ボタンを用意してください。

詳細編集では、ダイアログまたは画面右側の詳細編集パネルを開き、その在庫データの全項目を編集できるようにしてください。

詳細編集で編集可能な項目は以下です。

- ジャンル
- カード名
- レア
- セット
- 言語
- コレクター番号
- 補足
- カード状態
- 枚数
- 買取価格
- 販売価格
- 在庫ステータス
- 最終確認日
- メモ

詳細編集でも、在庫ID、登録日、更新日は編集不可にしてください。

在庫IDは詳細編集画面で確認用に表示してください。ただし、詳細編集でも編集不可にしてください。

詳細編集画面には、保存ボタンとキャンセルボタンを必ず用意してください。

保存ボタンを押すと入力内容を保存し、更新日を当日の日付へ自動更新してください。

キャンセルボタンを押すと編集内容を破棄し、元の表示に戻してください。

一覧上のクイック編集は、補足・価格・枚数・ステータス・最終確認日など、日常的に変更頻度が高い項目を素早く編集する用途としてください。

セット、言語、カード状態、コレクター番号、メモなどの詳細項目は、詳細編集から編集できる仕様にしてください。

# 複製機能

同じカード名の別バージョン、別言語、別状態、別価格の商品を登録しやすくするため、在庫一覧の各行の操作欄に「複製」ボタンを用意してください。

複製ボタンを押すと、対象行の内容を元に、新しい在庫行を作成してください。

複製時は以下の項目を元データから引き継いでください。

- ジャンル
- カード名
- レア
- セット
- 言語
- コレクター番号
- 補足
- カード状態
- 枚数
- 買取価格
- 販売価格
- 在庫ステータス
- メモ

複製時は、在庫IDを新しく自動採番してください。

登録日、最終確認日、更新日は、複製した当日の日付を設定してください。

複製後は、作成した新しい行をクイック編集状態にして、補足、枚数、販売価格、最終確認日、在庫ステータスをすぐ修正できるようにしてください。
カード状態、言語、セットなど複製後に詳細項目を変更したい場合は、詳細編集から変更できるようにしてください。

未保存のクイック編集がある状態で複製ボタンを押した場合は、編集ボタンや詳細編集ボタンと同じ確認ダイアログを表示してください。

# 削除・ステータス管理

初期版では、物理削除は行わないでください。

削除操作をした場合は、在庫ステータスを「削除済み」に変更してください。

個別の削除ボタンは設けないでください。

削除済みへの変更は、クイック編集、詳細編集、または在庫一覧上の一括操作からのみ行う仕様にしてください。

一括操作で削除済みにする場合は、確認ダイアログを表示してください。

確認メッセージ例：

「選択した在庫を削除済みに変更します。よろしいですか？」

売れた商品については、在庫ステータスを「売却済み」に変更できるようにしてください。

在庫一覧の各行には、売却済みボタンや削除ボタンを表示しないでください。

売却済みや削除済みへの変更は、クイック編集、詳細編集、または在庫一覧上の一括操作から行う仕様にしてください。

一覧上でチェックを付けた商品に対する一括操作として、「売却済みにする」「削除済みにする」は残してください。

一括売却済み、一括削除済みを実行する前には確認ダイアログを表示してください。

# 一括操作機能

一覧画面で商品にチェックを付けて、一括操作できるようにしてください。

一括操作専用の別タブは作らず、在庫一覧画面上で操作を完結できるようにしてください。

一括操作として以下を用意してください。

- 最終確認日を今日にする
- 売却済みにする
- 削除済みにする

一括で販売価格を同じ価格に変更する機能は、初期版では不要です。

一括操作を実行する前に、確認ダイアログを表示してください。

一括操作後は、対象商品の更新日を当日の日付へ更新してください。

# 絞り込み機能

一覧画面の上部に絞り込みエリアを作成してください。

一覧を見やすくするため、絞り込みエリアは折りたたみ可能にしてください。

初期表示では絞り込みエリアを閉じた状態にし、ユーザーが「絞り込み」または「条件を開く」ボタンを押すと展開できるようにしてください。

絞り込み条件を適用中の場合は、折りたたみ時でも条件が適用されていることが分かる表示にしてください。

以下の項目で絞り込みできるようにしてください。

- ジャンル
- カード名
- レア
- セット
- 言語
- コレクター番号
- 補足
- 在庫ステータス
- 買取価格の範囲
- 登録日の範囲
- 最終確認日の範囲
- 更新日の範囲

テキスト項目は部分一致で検索できるようにしてください。

日付は範囲指定できるようにしてください。

初期版の一覧上部に常時表示する絞り込み項目は、実使用で検索頻度が高い項目を優先してください。

カード状態と販売価格の範囲検索は、初期版では常時表示の絞り込み項目に含めなくて構いません。

代わりに、コレクター番号と補足は一覧上部の絞り込み項目として必ず表示してください。

絞り込みボタンを押すと、条件に一致する商品だけ表示してください。

絞り込み解除ボタンを押すと、全ての絞り込み条件を解除してください。

初期版では、入力サジェスト機能は不要です。

## 絞り込み時の文字比較

カード名、セット、コレクター番号、補足、メモなどのテキスト入力による絞り込みでは、検索語と対象文字列の両方を検索用に正規化してから比較してください。

検索用の正規化では、以下を同一視してください。

- 大文字・小文字
- 英数字の全角・半角
- 一般的な記号の全角・半角

例：

- `lightning` で検索した場合、`Lightning Bolt` も一致する
- `ｓｖ４ａ` で検索した場合、`SV4a` も一致する
- `349／190` で検索した場合、`349/190` も一致する
- `Foil`、`Ｆｏｉｌ`、`foil` は同じ検索語として扱う

絞り込みは部分一致で行ってください。

正規化は絞り込み時の比較にのみ使用し、保存データ、画面表示、CSVインポート値、CSVエクスポート値は変更しないでください。

初期版では、ひらがな・カタカナ、濁点有無、長音記号の揺れまでは同一視しなくてよいです。

# 価格見直し用の絞り込み

価格見直しのために、最終確認日を基準にした絞り込みを用意してください。

以下の条件で絞り込めるようにしてください。

- 30日以上未確認
- 90日以上未確認
- 任意の日数以上未確認

判定には「更新日」ではなく「最終確認日」を使用してください。

価格を確認しただけで価格変更しない場合もあるため、一括操作で「最終確認日を今日にする」を使えるようにしてください。

# ソート機能

一覧画面でソートできるようにしてください。

以下の項目でソート可能にしてください。

- ジャンル
- カード名
- レア
- セット
- 言語
- カード状態
- 枚数
- 買取価格
- 販売価格
- 登録日
- 最終確認日
- 更新日
- 在庫ステータス

昇順・降順を選べるようにしてください。

# 価格検索リンク機能

在庫一覧の各商品に、外部サイトで価格確認するためのリンクを表示してください。

商品追加画面でも、入力中の商品情報を使って同じ価格検索リンクを表示してください。

リンクは別タブまたは既定ブラウザで開いてください。

価格検索リンクは、設定画面で管理できるようにしてください。

検索リンクはURLテンプレート方式にしてください。

検索語テンプレートから検索語を生成し、URLエンコードした値を `{query}` として検索URLテンプレートへ埋め込んでください。

例：

- 検索語テンプレート：`{card_name} {set} {collector_number}`
- 検索URLテンプレート：`https://example.com/search?q={query}`

使用できる変数は以下です。

- {genre}
- {card_name}
- {set}
- {language}
- {collector_number}
- {condition}
- {note}
- {query}

検索語を作る場合は、以下のように組み合わせられるようにしてください。

例：

{card_name} {set} {collector_number}

作成した検索語はURLエンコードして、URLに反映してください。

設定項目は以下にしてください。

- ジャンル
- サイト名
- 検索URLテンプレート
- 検索語テンプレート
- 有効/無効

想定する検索サイトは以下です。

- 晴れる屋
  - https://www.hareruyamtg.com/ja/
- カードラッシュ ポケモン
  - https://www.cardrush-pokemon.jp/

初期版では、完全なスクレイピングや価格自動取得は不要です。
外部サイトを開いて、人間が価格確認できれば問題ありません。

# CSVインポート機能

CSVから在庫データをインポートできるようにしてください。

CSVインポートは、原則として新規追加扱いにしてください。
既存データの上書きはしないでください。

CSV内に在庫IDがあっても、初期版では既存データとの照合・上書きはしないでください。
CSVインポート時は、CSV内の在庫IDの有無に関わらず、アプリ側で新しい在庫IDを採番してください。
CSV内の在庫IDは初期版では取り込まないでください。

CSVインポート時、完全に同一条件の商品がある場合は、可能であれば枚数を合算できる選択肢を用意してください。

ただし、初期版で複雑になる場合は、常に新規行として追加で構いません。

CSVの解析は、カンマやダブルクォートを含むカード名、セット名、メモに対応できるCSVパーサーを使用してください。
単純なカンマ分割だけでCSVを解析しないでください。

## CSVインポート形式の自動判別

CSVインポート時は、ヘッダー行から以下の形式を自動判別してください。

- 通常CSV
- Manabox CSV

通常CSVは、このアプリのCSV仕様に従って取り込んでください。

Manabox CSVは、ヘッダーに `Name`、`Set code`、`Collector number`、`Foil`、`Rarity`、`Quantity`、`Language` が含まれる場合にManabox形式として扱ってください。

判別できないCSVの場合は、取り込み前にエラーとして表示してください。

## Manabox CSVインポート

Manabox CSVを読み込む場合は、Manabox側の列を以下の在庫項目へ変換してください。

- `Name` → カード名
- `Set code` → セット
- `Collector number` → コレクター番号
- `Foil` → 補足
- `Rarity` → レア
- `Quantity` → 枚数
- `Language` → 言語

Manabox CSVにはジャンルがないため、ジャンルは `MTG` を設定してください。

Manabox CSVには販売価格がないため、販売価格は `0` を設定してください。

Manabox CSVには在庫ステータスがないため、在庫ステータスは `準備中` を設定してください。

Manabox CSVには登録日、最終確認日、更新日がないため、取り込み日を設定してください。

Manabox CSVの `Condition` と `Purchase price` は、初期版では取り込まなくて構いません。

Manabox CSVの `Foil` は以下のように変換して補足へ格納してください。

- `foil` / `Foil` → `#Foil`
- `normal` / `Normal` → 空欄
- その他 → 元の文字列

Manabox CSVの `Rarity` は、大文字・小文字を区別せずに以下のように変換してレアへ格納してください。

- `common` / `Common` → `C`
- `uncommon` / `Uncommon` → `U`
- `rare` / `Rare` → `R`
- `mythic` / `Mythic` → `M`
- その他 → 元の文字列

Manabox CSVの `Language` は以下のように変換してください。

- `ja` → `jp`
- その他 → 元の文字列

Manabox CSV内の `ManaBox ID`、`Scryfall ID`、`Set name`、`Misprint`、`Altered`、`Purchase price currency` など、対応する在庫項目がない列は初期版では取り込まなくて構いません。

## CSVインポート時のエラー処理

CSVインポート時に以下の問題がある場合は、取り込み前にエラー一覧を表示してください。

- 必須項目不足
- 価格の数値不正
- 枚数の数値不正
- 日付変換失敗
- CSV列不足
- 文字コード読み込み失敗

エラーがある場合は、いきなり取り込まず、ユーザーが確認できるようにしてください。

エラー行をスキップして取り込むか、全体をキャンセルするか選べると望ましいです。

# CSVエクスポート機能

SQLiteに保存されている在庫データをCSVでエクスポートできるようにしてください。

エクスポート対象は、現在の絞り込み条件を反映できるようにしてください。

例：

- 販売中のみエクスポート
- MTGのみエクスポート
- 30日以上未確認のみエクスポート
- 全件エクスポート

CSVの日付形式は YYYY-MM-DD にしてください。

# CSV仕様

CSVの列は以下を基本にしてください。

在庫ID,ジャンル,カード名,レア,セット,言語,コレクター番号,補足,カード状態,枚数,買取価格,販売価格,在庫ステータス,登録日,最終確認日,更新日,メモ

例：

在庫ID,ジャンル,カード名,レア,セット,言語,コレクター番号,補足,カード状態,枚数,買取価格,販売価格,在庫ステータス,登録日,最終確認日,更新日,メモ
INV-000001,MTG,Lightning Bolt,C,4EDBB,jp,,通常,MP,1,1000,2100,販売中,2026-03-01,2026-03-01,2026-03-01,

# 設定画面

設定画面では以下を管理できるようにしてください。

## 価格検索リンク設定

- ジャンル
- サイト名
- 検索URLテンプレート
- 検索語テンプレート
- 有効/無効

複数の検索リンクを設定できるようにしてください。

## 表示設定

一覧画面で表示する列を設定できるようにしてください。

初期表示列は以下です。

- ジャンル
- カード名
- 補足
- 枚数
- 販売価格
- 最終確認日
- 在庫ステータス
- 価格検索リンク

カード名の下には、サブ情報としてセット、レア、カード状態、言語、コレクター番号を表示してください。

# 初期版では実装しない機能

以下は便利だが、初期版では実装しなくてよいです。

- 高度な入力サジェスト
- 補足テキストボックスのタグ候補フロート表示
- 他項目との一致率を考慮した候補表示
- 外部サイトからの価格自動取得
- スクレイピング
- QRコード管理
- バーコード管理
- チェックした商品の買取見積書作成、合計金額表示、印刷
- 複雑なセット別チェック期間管理
- 顧客別の買取履歴管理
- 複数端末同期
- クラウド保存
- 権限管理
- 印刷ラベル作成

まずは、ローカルPC上で安定して動く在庫管理アプリを目指してください。

# 実装時の注意

- データ保存前にバリデーションを行ってください
- 価格・枚数は数値として扱ってください
- 日付は YYYY-MM-DD に統一してください
- SQLiteのテーブル設計を明確にしてください
- 後から機能追加しやすいように、処理を分けてください
- CSVインポート時にデータ破損しないようにしてください
- 削除は物理削除ではなく、在庫ステータス変更にしてください
- 編集時は保存とキャンセルを明確に分けてください
- 価格検索リンクはURLエンコードしてください
- エラーが発生した場合は、ユーザーに分かりやすく表示してください
- 補足欄は自由入力のまま維持し、将来的にFoil、拡張アート、プロモ、パラレル、カードの色などを候補表示して挿入できるUIを追加しやすい構成にしてください
- 将来的にチェック済み商品から買取見積書を作成できるよう、選択中商品の一覧、買取価格、枚数、小計、合計金額を扱いやすい構成にしてください
- ステータスを追加する場合は、絞り込み、CSVインポート・エクスポート、初期表示、削除・売却済み処理への影響を確認してください
- 商品追加画面の価格確認リンクは保存処理と分離し、未保存の入力内容から検索URLだけを生成してください

# 希望する成果物

以下を作成してください。

1. アプリ本体のコード
2. SQLiteのテーブル作成処理
3. サンプルCSV
4. README
5. 起動方法
6. 将来的に.exe化する場合の手順メモ

# まず最初に行ってほしいこと

いきなり実装せず、まず以下を提示してください。

1. 推奨する技術構成
2. 画面構成
3. SQLiteのテーブル設計
4. フォルダ構成
5. 実装手順

その後、こちらが確認してから実装に進める形にしてください。
