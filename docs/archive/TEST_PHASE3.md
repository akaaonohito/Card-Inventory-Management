# Phase 3 動作確認テスト項目

## 1. 作業モード切り替え

| No | 確認内容 | 手順 | 期待結果 |
|---|---|---|---|
| 1-1 | 初期表示 | アプリを起動する | 前回選択した作業モード、または管理モードで表示される |
| 1-2 | 管理モード | 右上の作業モードで `管理モード` を選ぶ | 標準列が表示され、在庫ステータスが販売中で絞り込まれる |
| 1-3 | 買取モード | 右上の作業モードで `買取モード` を選ぶ | 買取向けの列が表示され、在庫ステータスが準備中で絞り込まれる |
| 1-4 | 価格見直しモード | 右上の作業モードで `価格見直しモード` を選ぶ | 価格と確認日向けの列が表示され、在庫ステータスが販売中で絞り込まれる |
| 1-5 | 未保存編集の保護 | クイック編集中に作業モードを切り替える | 未保存編集を破棄するか確認される |

## 2. モード別表示設定

| No | 確認内容 | 手順 | 期待結果 |
|---|---|---|---|
| 2-1 | 設定対象切替 | 設定タブで設定対象モードを切り替える | 選んだモードの表示列と初期ステータスが表示される |
| 2-2 | 表示列保存 | 買取モードの表示列を変更して保存する | 買取モードに切り替えたときだけ変更した列になる |
| 2-3 | 初期ステータス保存 | 買取モードの初期ステータスを販売中にして保存する | 買取モード切り替え時のステータス絞り込みが販売中になる |
| 2-4 | 空列防止 | 全ての表示列チェックを外して保存する | エラーが表示され、保存されない |
| 2-5 | 買取モード専用検索先 | 価格検索リンク設定で適用モードを `買取モードのみ` にした検索先を保存し、買取モードで価格検索を開く | 買取モード専用の検索先が開く |
| 2-6 | 通常モード検索先 | 管理モードまたは価格見直しモードで価格検索を開く | `買取モードのみ` の検索先は使われず、全モード用の検索先が開く |

## 3. 既存機能への影響

| No | 確認内容 | 手順 | 期待結果 |
|---|---|---|---|
| 3-1 | 管理モードのクイック編集 | 管理モードで行をダブルクリックする | 表示列のうち、補足・枚数・販売価格・最終確認日・ステータスだけが編集対象になる |
| 3-2 | 買取モードのクイック編集 | 買取モードで行をダブルクリックする | 表示列のうち、カード名・レア・セット・コレクター番号・補足・カード状態・枚数・買取価格・メモ・ステータスが編集対象になる |
| 3-3 | 買取価格の入力検証 | 買取モードのクイック編集で買取価格に文字を入れて保存する | 数値不正のエラーが表示され、保存されない |
| 3-4 | カード名の入力検証 | 買取モードのクイック編集でカード名を空欄にして保存する | 必須項目エラーが表示され、保存されない |
| 3-5 | CSVエクスポート | 任意のモードでCSVエクスポートする | 現在の絞り込み結果だけがCSV出力される |
| 3-6 | 再起動後維持 | モードを変更してアプリを終了し、再起動する | 最後に選んだモードが復元される |

## 4. Manabox CSVインポート

| No | 確認内容 | 手順 | 期待結果 |
|---|---|---|---|
| 4-1 | 自動判別 | CSVタブから `manabox-scan-サンプル.csv` を選ぶ | `Manabox CSVとして読み込みました` と表示される |
| 4-2 | 値変換 | Manabox CSVを取り込む | ジャンルはMTG、ステータスは準備中、販売価格は0で登録される |
| 4-3 | Foil変換 | `Foil` が `foil` の行を取り込む | 補足が `#Foil` になる |
| 4-4 | Rarity変換 | `Rarity` が `rare` の行を取り込む | レアが `R` になる |
| 4-5 | Language変換 | `Language` が `ja` の行を取り込む | 言語が `jp` になる |
| 4-6 | 未対応CSV | 必須列が足りないCSVを選ぶ | 取り込み前にCSV形式を判別できないエラーが表示される |

## 5. 通常CSV日付変換

| No | 確認内容 | 手順 | 期待結果 |
|---|---|---|---|
| 5-1 | スラッシュ日付 | 通常CSVの日付に `2026/03/01` を入れて取り込む | `2026-03-01` として登録される |
| 5-2 | 年なし日付 | 通常CSVの日付に `03/01` を入れて取り込む | 取り込み年の `YYYY-03-01` として登録される |
| 5-3 | 日本語日付 | 通常CSVの日付に `3月1日` を入れて取り込む | 取り込み年の `YYYY-03-01` として登録される |

## 簡易コマンド確認

| No | 確認内容 | コマンド | 期待結果 |
|---|---|---|---|
| 6-1 | Python構文チェック | `python -m compileall app` | エラーなく完了する |
