# Phase 1 動作確認テスト項目

Phase 1で実装した基本機能が正常に動作するか確認するためのテスト項目です。

## 事前準備

- `run_app.bat` または `python -m app.main` でアプリを起動できること
- `sample_inventory.csv` が存在すること
- 初回起動時に `data/inventory.sqlite3` が作成されること
- テスト前に必要であれば、既存の `data/inventory.sqlite3` を退避してから実施すること

## 1. アプリ起動

| No | 確認内容 | 手順 | 期待結果 |
|---|---|---|---|
| 1-1 | bat起動 | `run_app.bat` をダブルクリックする | アプリ画面が表示される |
| 1-2 | コマンド起動 | `python -m app.main` を実行する | アプリ画面が表示される |
| 1-3 | DB自動作成 | 初回起動後に `data/inventory.sqlite3` を確認する | SQLite DBファイルが作成されている |
| 1-4 | 画面タブ表示 | 起動後のタブを確認する | `在庫一覧`、`商品追加`、`CSV` タブが表示される |

## 2. 商品追加

| No | 確認内容 | 手順 | 期待結果 |
|---|---|---|---|
| 2-1 | 最小必須項目で追加 | ジャンル、カード名、枚数、販売価格、ステータス、最終確認日を入力して保存する | 在庫が追加され、在庫一覧に表示される |
| 2-2 | 在庫ID自動採番 | 商品を追加後、一覧の在庫IDを確認する | `INV-000001` 形式のIDが自動採番される |
| 2-3 | 任意項目ありで追加 | レア、セット、言語、コレクター番号、補足、カード状態、買取価格、メモも入力して保存する | 入力した内容が保存される |
| 2-4 | 最終確認日未入力 | 最終確認日を空欄にして保存する | 登録日と同じ日付が自動設定される |
| 2-5 | ステータス初期値 | 商品追加画面を開く | 在庫ステータスが `販売中` になっている |
| 2-6 | 枚数初期値 | 商品追加画面を開く | 枚数が `1` になっている |
| 2-7 | 販売価格初期値 | 商品追加画面を開く | 販売価格が `0` になっている |

## 3. 商品追加の入力エラー

| No | 確認内容 | 手順 | 期待結果 |
|---|---|---|---|
| 3-1 | ジャンル必須 | ジャンルを空欄にして保存する | エラーが表示され、保存されない |
| 3-2 | カード名必須 | カード名を空欄にして保存する | エラーが表示され、保存されない |
| 3-3 | 枚数必須 | 枚数を空欄にして保存する | エラーが表示され、保存されない |
| 3-4 | 枚数数値チェック | 枚数に `abc` を入力して保存する | エラーが表示され、保存されない |
| 3-5 | 枚数負数チェック | 枚数に `-1` を入力して保存する | エラーが表示され、保存されない |
| 3-6 | 販売価格必須 | 販売価格を空欄にして保存する | エラーが表示され、保存されない |
| 3-7 | 販売価格数値チェック | 販売価格に `abc` を入力して保存する | エラーが表示され、保存されない |
| 3-8 | 買取価格任意 | 買取価格を空欄にして保存する | エラーにならず保存される |
| 3-9 | 日付形式チェック | 最終確認日に `2026/03/01` を入力して保存する | エラーが表示され、保存されない |
| 3-10 | 不正日付チェック | 最終確認日に `2026-02-31` を入力して保存する | エラーが表示され、保存されない |

## 4. 在庫一覧

| No | 確認内容 | 手順 | 期待結果 |
|---|---|---|---|
| 4-1 | 販売中初期表示 | アプリを起動して在庫一覧を確認する | `販売中` の在庫が表示される |
| 4-2 | ステータス絞り替え | 表示ステータスを `準備中` に変更して更新する | `準備中` の在庫のみ表示される |
| 4-3 | 全ステータス表示 | 表示ステータスを空欄にして更新する | 全ステータスの在庫が表示される |
| 4-4 | 一覧列表示 | 一覧の列を確認する | 在庫ID、ジャンル、カード名、補足、枚数、販売価格、最終確認日、在庫ステータスが表示される |
| 4-5 | 更新ボタン | DB登録済みの状態で更新ボタンを押す | 一覧が再読み込みされる |

## 5. クイック編集

| No | 確認内容 | 手順 | 期待結果 |
|---|---|---|---|
| 5-1 | クイック編集開始 | 在庫を選択して `クイック編集` を押す | 選択行の補足、枚数、販売価格、最終確認日、在庫ステータスが一覧上で入力欄に切り替わる |
| 5-2 | クイック編集項目 | クイック編集状態の行を確認する | ジャンル、カード名、在庫IDは入力欄にならず、詳細編集用の項目は表示のままになっている |
| 5-3 | クイック編集保存 | 補足、枚数、販売価格を変更して保存する | 一覧に変更内容が反映される |
| 5-4 | ステータス変更 | 在庫ステータスを `売却済み` に変更して保存する | 表示ステータスを `売却済み` にすると対象在庫が表示される |
| 5-5 | 更新日自動更新 | クイック編集で保存後、詳細編集を開いて更新日を確認する | 更新日が当日の日付になっている |
| 5-6 | キャンセル | クイック編集で値を変更してキャンセルする | 変更内容が破棄され、元の一覧表示に戻る |
| 5-7 | ダブルクリック編集 | 一覧の行をダブルクリックする | 対象行がクイック編集状態になる |
| 5-8 | 未保存確認 | クイック編集で値を変更したまま別行のクイック編集または詳細編集を押す | 未保存内容を破棄するか確認するダイアログが表示される |

## 6. クイック編集の入力エラー

| No | 確認内容 | 手順 | 期待結果 |
|---|---|---|---|
| 6-1 | 数値項目空欄不可 | クイック編集で枚数を空欄にして保存する | エラーが表示され、保存されない |
| 6-2 | 数値項目不正 | クイック編集で枚数または販売価格に `abc` を入力して保存する | エラーが表示され、保存されない |
| 6-3 | 日付空欄不可 | クイック編集で最終確認日を空欄にして保存する | エラーが表示され、保存されない |
| 6-4 | 補足空欄保存 | クイック編集で補足を空欄にして保存する | エラーにならず、補足が空欄で保存される |

## 7. 詳細編集

| No | 確認内容 | 手順 | 期待結果 |
|---|---|---|---|
| 7-1 | 詳細編集画面 | 在庫を選択して `詳細編集` を押す | 詳細編集ダイアログが開く |
| 7-2 | 読み取り専用項目表示 | 詳細編集画面の上部を確認する | 在庫ID、登録日、更新日が確認用に表示される |
| 7-3 | 詳細編集項目 | 詳細編集ダイアログの項目を確認する | レア、セット、言語、コレクター番号、カード状態、買取価格、メモなどが編集できる |
| 7-4 | 詳細編集保存 | セット、言語、カード状態、買取価格、メモを変更して保存する | 変更内容が保存される |
| 7-5 | 詳細編集キャンセル | 詳細編集で値を変更してキャンセルする | 変更内容が破棄される |
| 7-6 | 更新日自動更新 | 詳細編集で保存後、再度詳細編集を開く | 更新日が当日の日付になっている |

## 8. 通常CSVインポート

| No | 確認内容 | 手順 | 期待結果 |
|---|---|---|---|
| 8-1 | サンプルCSV取込 | CSVタブから `sample_inventory.csv` をインポートする | CSV内の在庫が追加される |
| 8-2 | 在庫ID新規採番 | CSV内の在庫IDが空欄または既存値のCSVを取り込む | アプリ側で新しい在庫IDが採番される |
| 8-3 | 既存データ非上書き | 同じCSVを2回インポートする | 既存データは上書きされず、新規行として追加される |
| 8-4 | UTF-8 BOM CSV | UTF-8 BOM付きCSVをインポートする | 正常に取り込まれる |
| 8-5 | CP932 CSV | CP932で保存したCSVをインポートする | 正常に取り込まれる |
| 8-6 | カンマ入りカード名 | カード名にカンマを含むCSVをインポートする | CSVパーサーで正しく取り込まれる |
| 8-7 | ダブルクォート入りメモ | メモにダブルクォートを含むCSVをインポートする | CSVパーサーで正しく取り込まれる |
| 8-8 | バックアップ作成 | CSVインポート後に `data/backups/` を確認する | インポート前のDBバックアップが作成されている |

## 9. CSVインポートのエラー処理

| No | 確認内容 | 手順 | 期待結果 |
|---|---|---|---|
| 9-1 | 必須項目不足 | カード名が空欄のCSVをインポートする | 取り込み前にエラーが表示される |
| 9-2 | 価格不正 | 販売価格に `abc` が入ったCSVをインポートする | 取り込み前にエラーが表示される |
| 9-3 | 枚数不正 | 枚数に `abc` が入ったCSVをインポートする | 取り込み前にエラーが表示される |
| 9-4 | 日付不正 | 登録日に `2026/03/01` が入ったCSVをインポートする | 取り込み前にエラーが表示される |
| 9-5 | CSV列不足 | 必須列が不足したCSVをインポートする | エラーが表示され、取り込まれない |
| 9-6 | エラー行スキップ | 正常行とエラー行が混在するCSVをインポートし、スキップを選ぶ | 正常行のみ取り込まれる |
| 9-7 | エラー時キャンセル | 正常行とエラー行が混在するCSVをインポートし、キャンセルを選ぶ | 1件も取り込まれない |

## 10. CSVエクスポート

| No | 確認内容 | 手順 | 期待結果 |
|---|---|---|---|
| 10-1 | 販売中エクスポート | 表示ステータスを `販売中` にしてCSVエクスポートする | 販売中の在庫のみCSV出力される |
| 10-2 | 全件エクスポート | 表示ステータスを空欄にしてCSVエクスポートする | 全ステータスの在庫がCSV出力される |
| 10-3 | ヘッダー確認 | エクスポートしたCSVを開く | 指定のCSVヘッダー順で出力されている |
| 10-4 | 日付形式確認 | エクスポートしたCSVの日付列を確認する | `YYYY-MM-DD` 形式で出力されている |
| 10-5 | 日本語文字化け確認 | エクスポートCSVをExcelで開く | 日本語が文字化けしない |

## 11. ステータス管理・削除済み運用

| No | 確認内容 | 手順 | 期待結果 |
|---|---|---|---|
| 11-1 | 売却済み変更 | クイック編集または詳細編集でステータスを `売却済み` に変更する | 物理削除されず、売却済みとして残る |
| 11-2 | 削除済み変更 | クイック編集または詳細編集でステータスを `削除済み` に変更する | 物理削除されず、削除済みとして残る |
| 11-3 | 削除済み表示 | 表示ステータスを `削除済み` にして更新する | 削除済みの在庫が表示される |
| 11-4 | 個別削除ボタンなし | 在庫一覧の操作ボタンを確認する | 個別の削除ボタンは表示されない |

## 12. データ永続化

| No | 確認内容 | 手順 | 期待結果 |
|---|---|---|---|
| 12-1 | 再起動後保持 | 商品追加後にアプリを終了し、再起動する | 追加した在庫が残っている |
| 12-2 | 編集後保持 | 商品編集後にアプリを終了し、再起動する | 編集した内容が残っている |
| 12-3 | CSV取込後保持 | CSVインポート後にアプリを終了し、再起動する | 取り込んだ在庫が残っている |

## 13. ファイル除外確認

| No | 確認内容 | 手順 | 期待結果 |
|---|---|---|---|
| 13-1 | DBのGit除外 | `git status --short --ignored` を確認する | `data/` がignoreされ、DBがコミット対象にならない |
| 13-2 | Pythonキャッシュ除外 | アプリ実行後に `__pycache__` ができた状態でGit状態を確認する | `__pycache__` がコミット対象にならない |

## 14. 簡易コマンド確認

| No | 確認内容 | コマンド | 期待結果 |
|---|---|---|---|
| 14-1 | Python構文チェック | `python -m compileall app` | エラーなく完了する |
| 14-2 | DB初期化 | `python -c "from app.database import initialize_database; initialize_database(); print('ok')"` | `ok` が表示される |
| 14-3 | CSVプレビュー | `python -c "from app.csv_io import preview_import; rows, errors = preview_import('sample_inventory.csv'); print(len(rows), len(errors))"` | `2 0` が表示される |

## Phase 1対象外

以下はPhase 2以降の機能のため、Phase 1の合格条件には含めません。

- 詳細な絞り込み機能
- ソート機能
- 価格見直し用の絞り込み
- 一括操作
- 複製機能
- 価格検索リンク
- 価格検索リンク設定
- 表示列の選択
- Manabox CSVインポート
- CSV形式の自動判別
